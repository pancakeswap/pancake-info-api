/// <reference types="node" />
import { NowRequest, NowResponse } from './types';
import { Server } from 'http';
import { Bridge } from './bridge';
export declare class ApiError extends Error {
    readonly statusCode: number;
    constructor(statusCode: number, message: string);
}
export declare function sendError(res: NowResponse, statusCode: number, message: string): void;
export declare function createServerWithHelpers(listener: (req: NowRequest, res: NowResponse) => void | Promise<void>, bridge: Bridge): Server;
