import { FileFsRef, PrepareCacheOptions, BuildOptions, shouldServe } from '@now/build-utils';
export { NowRequest, NowResponse } from './types';
export declare const version = 2;
export declare const config: {
    maxLambdaSize: string;
};
export declare function build({ files, entrypoint, workPath, config, meta, }: BuildOptions): Promise<{
    output: {
        [x: string]: import("@now/build-utils/dist").Lambda;
    };
    watch: string[];
}>;
export declare function prepareCache({ workPath }: PrepareCacheOptions): Promise<{
    [x: string]: FileFsRef;
}>;
export { shouldServe };
