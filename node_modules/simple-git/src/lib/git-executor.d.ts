import { SimpleGitTask } from './tasks/task';
import { GitExecutorEnv, outputHandler } from './types';
export declare class GitExecutor {
    binary: string;
    cwd: string;
    env?: GitExecutorEnv;
    outputHandler?: outputHandler | undefined;
    private _chain;
    constructor(binary: string, cwd: string, env?: GitExecutorEnv, outputHandler?: outputHandler | undefined);
    push<R>(task: SimpleGitTask<R>): Promise<void | R>;
    private handleTaskData;
    private gitResponse;
}
